@model IEnumerable<Cinema_BD2.Models.District>

@{
    ViewData["Title"] = "Distritos";
}

<div class="text-center">
    <h1 class="display-4">
        @(Model?.Count() ?? 0) Distrito(s).
    </h1>

    @if (!Model.Any())
    {
        <h1>Nenhum Distrito cadastrado.</h1>
    }

    <a asp-controller="District" asp-action="Create">Novo</a>
    <table class="table table-responsive table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Distrito</th>
                <th>Editar</th>
                <th>Excluir</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var d in Model)
            {
                <tr>
                    <td>@d.Id</td>
                    <td>@d.Name</td>
                    <td>
                        <a asp-controller="District" asp-action="Edit" class="btn btn-primary btn-sm" asp-route-id="@d.Id">
                            Editar
                        </a>
                    </td>
                    <td>
                        <form asp-controller="District" asp-action="Delete" method="post">
                            <input type="hidden" name="id" value="@d.Id" />
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir este distrito?');">
                                Excluir
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


