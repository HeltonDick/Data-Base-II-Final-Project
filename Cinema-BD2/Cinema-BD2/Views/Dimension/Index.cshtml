@model IEnumerable<Cinema_BD2.Models.Dimension>

@{
    ViewData["Title"] = "Dimensões";
}

<div class="text-center">
    <h1 class="display-4">
        @(Model?.Count() ?? 0) Dimensão(ões).
    </h1>

    @if (!Model.Any())
    {
        <h1>Nenhuma dimensão cadastrada.</h1>
    }

    <!--
    <form asp-controller="Dimension" asp-action="Index" method="get" class="d-flex mb-4">
        <input type="text" name="search" class="form-control me-2"
               placeholder="Pesquisar Dimensão pelo nome..." />

        <button type="submit" class="btn btn-primary me-2">
            <i class="bi bi-search"></i>
        </button>

        <a asp-controller="Dimension" asp-action="Search" class="btn btn-secondary">
            <i class="bi bi-x-circle"></i>
        </a>
    </form>
    -->

    <a asp-controller="Dimension" class="btn btn-primary" asp-action="Create">Novo</a>

    <hr />

    <table class="table table-responsive table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dimensão</th>
                <th>Editar</th>
                <th>Excluir</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var d in Model)
            {
                <tr>
                    <td>@d.Id</td>
                    <td>@d.Name</td>
                    <td>
                        <a asp-controller="Dimension" asp-action="Edit" class="btn btn-primary btn-sm" asp-route-id="@d.Id">
                            Editar
                        </a>
                    </td>
                    <td>
                        <form asp-controller="Dimension" asp-action="Delete" method="post">
                            <input type="hidden" name="id" value="@d.Id" />
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir esta Dimensão?');">
                                Excluir
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


