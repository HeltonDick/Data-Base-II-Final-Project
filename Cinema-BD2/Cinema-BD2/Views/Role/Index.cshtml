@model IEnumerable<Cinema_BD2.Models.Role>

@{
    ViewData["Title"] = "Cargos";
}

<div class="text-center">
    <h1 class="display-4">
        @(Model?.Count() ?? 0) Cargo(s).
    </h1>

    @if (!Model.Any())
    {
        <h1>Nenhum Cargo cadastrado.</h1>
    }

    <a asp-controller="Role" asp-action="Create">Novo</a>
    <table class="table table-responsive table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cargo</th>
                <th>Editar</th>
                <th>Excluir</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.Id</td>
                    <td>@r.Name</td>
                    <td>
                        <a asp-controller="Role" asp-action="Edit" class="btn btn-primary btn-sm" asp-route-id="@r.Id">
                            Editar
                        </a>
                    </td>
                    <td>
                        <form asp-controller="Role" asp-action="Delete" method="post">

                            <input type="hidden" name="id" value="@r.Id" />
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir este Cargo?');">
                                Excluir
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


