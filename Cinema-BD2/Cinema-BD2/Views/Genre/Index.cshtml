@model IEnumerable<Cinema_BD2.Models.Genre>

@{
    ViewData["Title"] = "Generos";
}

<div class="text-center">
    <h1 class="display-4">
        @(Model?.Count() ?? 0) Gênero(s).
    </h1>

    @if (!Model.Any())
    {
        <h1>Nenhum Gênero cadastrado.</h1>
    }

    <!--
    <form asp-controller="Genre" asp-action="Index" method="get" class="d-flex mb-4">
        <input type="text" name="search" class="form-control me-2"
               placeholder="Pesquisar Gênero pelo nome..." />

        <button type="submit" class="btn btn-primary me-2">
            <i class="bi bi-search"></i>
        </button>

        <a asp-controller="Genre" asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-x-circle"></i>
        </a>
    </form>
    -->

    <a asp-controller="Genre" class="btn btn-primary" asp-action="Create">Novo</a>

    <hr />

    <table class="table table-responsive table-hover table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Genero</th>
                <th>Editar</th>
                <th>Excluir</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var g in Model)
            {
                <tr>
                    <td>@g.Id</td>
                    <td>@g.Name</td>
                    <td>
                        <a asp-controller="Genre" asp-action="Edit" class="btn btn-primary btn-sm" asp-route-id="@g.Id">
                            Editar
                        </a>
                    </td>
                    <td>
                        <form asp-controller="Genre" asp-action="Delete" method="post">
                            <input type="hidden" name="id" value="@g.Id" />
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Tem certeza que deseja excluir este Genero?');">
                                Excluir
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


